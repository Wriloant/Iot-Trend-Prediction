__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
.DS_Store

# Generated artifacts (regenerate with run_pipeline.py).
# Kept out of git to keep the repo light; raw data IS committed so the pipeline runs.
data/processed/
results/
# reports/figures/ are committed so the README + notebooks render on GitHub.
